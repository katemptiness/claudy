"""Platform-independent drawing.

`scene` decides what goes where in each of Claudy's windows and paints it
through a `Canvas` (see `canvas`), which each backend implements with its
own graphics API. `art` turns sprites and other pixel art into images.
"""
